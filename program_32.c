#include<stdio.h>
/// accept the num from user and check weather it is positive or negative

int main(){

int num;


printf("Enter the num ");/// print 
scanf("%d",&num);//// scan

if(num > 0)
{
    printf("The num is positive");
}

if(num < 0)
{
    printf("The num is negative");
}
a>=b

    return 0;
}